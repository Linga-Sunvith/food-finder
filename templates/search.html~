<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results - {{ query }}</title>
    <link rel="stylesheet" href="/static/styles.css">
</head>
<body>
    <div class="center-container">
        <h1>FoodFinder</h1>
        <p class="subline">Search results for "{{ query }}"</p>
        <div class="search-container">
            <form action="/search" method="get">
                <input type="text" name="query" id="search-bar" placeholder="Search for a recipe..." value="{{ query }}">
                <button type="submit" id="search-button">Search</button>
            </form>
        </div>

        {% if recipes|length == 0 %}
        <p class="not-found">No recipes found for "{{ query }}". Try searching for another dish!</p>
        {% else %}
        <div class="recipe-list">
            {% for recipe in recipes %}
            <div class="recipe-card">
                <h3>{{ recipe.name }}</h3>
                <p><strong>Description:</strong> {{ recipe.description }}</p>
                <p><strong>Ingredients:</strong> {{ recipe.ingredients | join(", ") }}</p>
                <p><strong>Time:</strong> {{ recipe.time }}</p>
                <p><strong>Instructions:</strong> <pre>{{ recipe.instructions }}</pre></p>
                <iframe width="560" height="315" src="{{ recipe.video }}" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            </div>
            {% endfor %}
        </div>
        {% endif %}
    </div>
</body>
</html>
